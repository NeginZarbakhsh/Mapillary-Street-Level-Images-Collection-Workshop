# Copy to .env and fill in. The SDK also accepts an `ant auth login` profile,
# in which case you can leave this unset entirely.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
CONTRACT_MODEL=claude-opus-5
CONTRACT_EFFORT=high
